# Pre-Implementation Brief Report: Public Home Page with Header Login Button

**Date**: 2026-07-15
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md)
**Roadmap**: [roadmap.md](../../../../.specify/memory/roadmap.md) · matched ledger entry **003 — Public Home Page with Header Login Button**
**Verdict**: ⚠️ PROCEED WITH UPDATES

---

## Summary

Briefed the active feature `specs/002-public-home-login-button/` against ledger entry 003 (matched
by its `spec dir:` pointer). The spec, plan, and tasks align tightly with the roadmap's recorded
outcome and scope — they were co-authored in the same session the entry was created, so there is
no substantive outcome/scope drift. Two pre-implementation considerations surfaced: (1) the
dependency 001 is ledger-status `specced` with 0/41 tasks checked, yet its auth surface is **already
present in the codebase** (middleware, `signOut` action, Supabase server/client, route constants) —
so 003 can build on real code, but the ledger and the checklist disagree about whether 001 is
"done"; (2) the entry's status is `specced`, and the user should move it to `in-progress` once
implementation starts. Proceed with the updates below.

---

## Surfaced Context *(the roadmap entry's recorded intent)*

- **Entry**: 003 — Public Home Page with Header Login Button  ·  **Status**: specced
- **Description**: Make the home page (`/`) public so visitors are not forced to log in, and add a
  state-aware authentication control to a shared site header — a "Log in" button (top-right) for
  anonymous visitors, and an account indicator + sign-out control for signed-in users.
- **Outcome (expected)**: Unauthenticated visitors reach the home page with no forced-login
  redirect; a visible "Log in" button in the header links to the login page; authenticated users see
  an account indicator + sign-out and are not bounced off the public home; protected (non-public)
  routes remain protected.
- **Scope (in / out)**:
  - *in*: Add `/` to `PUBLIC_ROUTES`; split `AUTH_PAGES` from `PUBLIC_ROUTES`; add a shared
    `SiteHeader` + `HeaderAuth` client component to the root layout; server-resolve the Supabase
    session in the layout to avoid a logged-out→logged-in flash; reuse the existing `signOut`
    action (no new server actions or contracts); remove the home-grown `<header>` baked into the
    `Advisor` feature component.
  - *out*: Social login, profile management, email verification (unchanged from 001); no new data
    entities, migrations, server actions, or HTTP contracts; no new `packages/*`.
- **Depends on**: 001 — amends the login feature's routing (the home page's protection status and
  the auth-account control are reactions to 001's behavior).
  - **Dependency status**: 001 is **specced** (not `abandoned`, not missing → valid). However it is
    not yet `implemented`/`verified` in the ledger (see F1).
- **Governed by**: C-02 (Next.js App Router, TypeScript strict, Tailwind-only, explicit
  server/client boundary), C-04 (Test-First via Playwright e2e, the user-specified test layer).
  No `ADR-NNNN` pointers → none to resolve (ADR directory `docs/adr/` is absent).
- **Addresses**: no PRD pointer recorded.
- **Related open questions / cross-cutting notes**: Q-01..Q-03 are project-level and unrelated to
  this spec. Cross-cutting note applies: the SDD workflow (`/speckit.spec` → … → implement) is
  mandatory; skipping stages is forbidden.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 🤔 | dependency state | 003 `depends-on` 001, which is ledger-status `specced` with 0/41 tasks checked — not `implemented`/`verified`. Yet 001's auth surface IS present in the codebase (`apps/web/src/middleware.ts`, `lib/auth/actions.ts` with `signOut`, `lib/supabase/server.ts` `createClient`, `lib/constants.ts` `PUBLIC_ROUTES`). The ledger/checklist say 001 isn't done; the code says it largely is. 003's tasks assume this surface (T020 reuses `createClient()`, T019 reuses `signOut` + `UserAvatar` + `SignOutButton`). | Treat the codebase as the source of truth for what 003 can build on (the auth surface exists), but reconcile 001's ledger status via `/speckit.roadmap.write`: if 001's code is effectively shipped, advance 001 to `implemented`/`verified`; otherwise note which tasks remain. Do not block 003 on a checklist that under-counts real progress. |
| F2 | 💡 | status readiness | 003 is `specced` and is about to start implementation (29 tasks, 0 done). The lifecycle next step is `in-progress`. | Apply the transition `specced` → `in-progress` via `/speckit.roadmap.write` when the first task (T001/T002) starts. (Status change is instruction-only here; this brief does not mutate it.) |
| F3 | 💡 | scope/outcome alignment | Spec, plan, and tasks match the entry's recorded outcome and (in/out) scope exactly — no drift to fix. This is the expected result since the entry was harvested from the spec in the same session. | None. Recorded as a clean check; nothing to change. |

**Drift categories checked and clean**: outcome-miss (none) · scope-creep (none) ·
constraint-violation (none — C-02/C-04 honored: strict TS, Tailwind-only, Playwright e2e Test-First)
· roadmap-stale (the entry is current as of this session's sync) · orphan-spec (resolved — the entry
now exists) · phantom-entry (n/a) · dependency-contradiction (001 exists, not abandoned) ·
superseded-ADR (no ADRs).

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 2 |
| 🤔 Question | 1 |
| **Total** | **3** |

## Recommended Actions *(proposed only — never auto-applied)*

1. **F2 (💡)**: When implementation starts, transition 003 from `specced` → `in-progress` via
   `/speckit.roadmap.write` (this brief does not mutate status).
2. **F1 (🤔)**: Reconcile 001's ledger status with reality via `/speckit.roadmap.write`. Its auth
   surface is in the codebase, so if it is effectively shipped, advance 001 to `implemented` (or
   `verified` once its debrief passes); if tasks remain, keep `in-progress` and check off what is
   done. This unblocks honest tracking of 003's dependency.
3. **F3 (💡)**: No action — alignment confirmed. Proceed to implement.

**Implementation grounding** (from the roadmap, for the implementer):
- Build on 001's real surface: `signOut` (`lib/auth/actions`), `createClient()`
  (`lib/supabase/server`), `PUBLIC_ROUTES` (`lib/constants`), existing `UserAvatar` + `SignOutButton`.
- Server-resolve the session in the root layout to avoid a logged-out→logged-in flash (FR-010).
- Test layer is **Playwright e2e** (user-specified); write red e2e first per C-04.

---

**Severity Legend**: 🎯 Must-Address (blocks proceeding) · 💡 Recommendation (strongly
suggested) · 🤔 Question (needs a decision). **This report is read-only**; apply any roadmap
changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.brief`.*