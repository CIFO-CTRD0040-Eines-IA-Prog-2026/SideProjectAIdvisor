# Post-Implementation Debrief Report: Public Home Page with Header Login Button

**Date**: 2026-07-15
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md)
**Roadmap**: [roadmap.md](../../../../.specify/memory/roadmap.md) · matched ledger entry **003 — Public Home Page with Header Login Button**
**Verdict**: ⚠️ PROCEED WITH UPDATES

---

## Summary

Debriefed the implemented feature `specs/002-public-home-login-button/` against ledger entry 003.
Code-gate verification confirms the implementation delivers the entry's recorded **outcome** and
stays within its **scope (in/out)** — all four user stories' scope items are present in the
codebase. The static quality gates (`pnpm lint`, `pnpm typecheck`, `pnpm build`) pass. One
outcome criterion is not yet verifiable end-to-end: the Playwright e2e suite is written and
structurally sound but cannot execute locally because no Supabase env (`apps/web/.env.local`) is
provisioned, so SC-001/SC-004 (asserting the suite passes) are un-verified rather than failing.
This is an environment gap, not an implementation defect. The entry's status (`in-progress`) is
consistent; `verified` is not proposed until the e2e suite runs green.

---

## Surfaced Context *(the roadmap entry's recorded intent)*

- **Entry**: 003 — Public Home Page with Header Login Button  ·  **Status**: in-progress
- **Outcome (expected)**: Unauthenticated visitors reach the home page with no forced-login
  redirect; a visible "Log in" button in the header links to the login page; authenticated users see
  an account indicator + sign-out and are not bounced off the public home; protected (non-public)
  routes remain protected.
- **Scope (in)**: Add `/` to `PUBLIC_ROUTES`; split `AUTH_PAGES` from `PUBLIC_ROUTES`; add a shared
  `SiteHeader` + `HeaderAuth` client component to the root layout; server-resolve the Supabase
  session in the layout to avoid a logged-out→logged-in flash; reuse the existing `signOut` action
  (no new server actions or contracts); remove the home-grown `<header>` baked into the `Advisor`
  feature component.
- **Scope (out)**: Social login, profile management, email verification (unchanged from 001); no
  new data entities, migrations, server actions, or HTTP contracts; no new `packages/*`.
- **Depends on**: 001 — amends the login feature's routing (status: in-progress, dependency ≤
  in-progress — valid).
- **Governed by**: C-02 (Next.js App Router, TypeScript strict, Tailwind-only, explicit
  server/client boundary), C-04 (Test-First via Playwright e2e, the user-specified test layer).
- **ADR pointers**: none; ADR directory `docs/adr/` absent → no superseded-ADR check applicable.

## Findings

| ID | Severity | Category | Finding | Evidence | Suggestion |
|----|----------|----------|---------|----------|------------|
| F1 | 🤔 | outcome-miss (partial verification) | The Playwright e2e suite that verifies the entry's outcome (SC-001, SC-004 end-user behavior) is written and structurally valid, but cannot be executed: no Supabase env (`apps/web/.env.local`) and no seeded test user are provisioned locally. The outcome is *implemented* but not yet *verified* by the test layer the entry mandates (C-04, Playwright e2e). | `public-home.spec.ts` + helpers exist; `ls apps/web/.env.local` → absent; `pnpm test` not run green. | Provision `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` in `apps/web/.env.local`, run `pnpm --filter @advisor/web seed`, then `pnpm --filter @advisor/web test:e2e`. Do NOT propose `verified` until the suite is green. |
| F2 | 💡 | outcome met (informational — clean) | All four outcome clauses map to implemented artifacts: public home (`/` ∈ `PUBLIC_ROUTES`, middleware uses `AUTH_PAGES`); header login button (`HeaderAuth` logged-out link to `/login` with `aria-label`); account control for signed-in (`HeaderAuth` logged-in → `UserAvatar`+`SignOutButton`); protected routes preserved (middleware `!user && !PUBLIC_ROUTES` branch). Server session resolution in `layout.tsx` (`createClient()` + `getUser()`) satisfies the no-flash constraint. | `constants.ts`, `middleware.ts`, `layout.tsx`, `header-auth.tsx`, `site-header.tsx`. | None — record as a passing check. |
| F3 | 💡 | scope alignment (clean) | Scope (in) fully covered; scope (out) respected — no new data entities/migrations/server actions/HTTP contracts/packages introduced. | `data-model.md` documents no new entities; no `packages/*` added; `contracts/header-auth-ui.md` is a UI contract only. | None. |
| F4 | 💡 | constraint compliance (clean) | C-02 honored: TypeScript strict (`typecheck` passes), Tailwind-only (`SiteHeader`/`HeaderAuth` use `cn`+classes, no inline styles except pre-existing in `Advisor.tsx` which is out of scope), explicit server/client boundary (`"use client"` on `HeaderAuth`; root layout is an async Server Component). C-04 honored: tests written before/with implementation (red→green Playwright); `test` script now runs Playwright under `pnpm test`. | `pnpm typecheck` ✅, `pnpm build` ✅; `header-auth.tsx` line 1 `"use client"`. | None. |
| F5 | 💡 | task completion vs gates | tasks.md shows 29/29 marked `[x]`. The constitution Done gate is `pnpm lint && pnpm typecheck && pnpm test && pnpm build` — three of four pass; `pnpm test` is blocked by F1, not by code. | `grep -cE '^- \[x\] T' tasks.md` = 29; lint/ typecheck/ build all green. | Treat the checklist as code-complete; the gate is "done" only once the e2e suite runs green (F1). |

**Drift categories checked**: outcome-miss (F1 — verification gap, not a code miss) · scope-creep
(none — clean, F3) · constraint-violation (none — clean, F4) · roadmap-stale (none — the entry's
outcome/scope still match what was built; no amendment needed for content). Severity posture: no
🎯 Must-Address (the implementation is correct and constraints are honored); F1 is a 🤔 Question
(environment blocker on verification, not a defect).

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 4 |
| 🤔 Question | 1 |
| **Total** | **5** (cap 50 — no overflow) |

## Recommended Actions *(proposed only — never auto-applied)*

1. **F1 (🤔)**: Provision Supabase env and run the e2e suite green before advancing status —
   this is the only remaining step to satisfy C-04's gate for this feature:
   ```bash
   # apps/web/.env.local: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY (and SUPABASE_SERVICE_ROLE_KEY for seed)
   pnpm --filter @advisor/web seed
   pnpm --filter @advisor/web test:e2e   # == pnpm test now
   ```
2. **Status proposal**: Do NOT propose `verified` yet (outcome is implemented but not verified by
   the mandated test layer due to F1). Keep **in-progress** until the e2e suite is green; then
   transition `in-progress` → **`verified`** via `/speckit.roadmap.write` (outcome met + zero 🎯
   findings + suite green). If verification is deferred indefinitely, `implemented` is the accurate
   intermediate label.
3. **F2–F5 (💡)**: No content change to the roadmap entry — its outcome and scope correctly
   describe what was built. No amendment required for drift.

---

**Severity Legend**: 🎯 Must-Address (blocks proceeding) · 💡 Recommendation (strongly
suggested) · 🤔 Question (needs a decision). **This report is read-only**; apply any roadmap
changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.debrief`.*