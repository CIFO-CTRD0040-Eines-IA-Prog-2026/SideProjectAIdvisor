# Roadmap Sync Report: Public Home Page with Header Login Button

**Date**: 2026-07-15
**Feature**: project-wide reconciliation (ledger vs `specs/` on disk)
**Roadmap**: [roadmap.md](../memory/roadmap.md) · ledger entries 001, 002 (no entry for the orphan 002-public-home-login-button)
**Verdict**: ⚠️ PROCEED WITH UPDATES

---

## Summary

Reconciled the roadmap ledger (`.specify/memory/roadmap.md`, 2 entries) against the two spec
directories in `specs/` and the ADR directory. The roadmap ledger is **stale**: a fully-specced
feature (`specs/002-public-home-login-button/` — spec, plan, research, data-model, contracts,
quickstart, and 29 tasks all present) has **no ledger entry at all** (orphan-spec), and its `002`
number collides with the ledger's pre-commitment `002 — Job Offer Analyzer` entry, which is about
a different feature. Entry 001 is disk-consistent. No phantom, no abandoned-but-active, no
dependency contradiction, and no superseded-ADR (ADR directory absent, so the ADR check is
skipped). Two must-address reconciling actions are proposed for `/speckit.roadmap.write`.

---

## Surfaced Context *(the two ledger entries + the orphan, as they stand)*

- **Ledger 001 — User Login**  ·  **Status**: in-progress
  - **Description**: Supabase Auth email/password sign-up/login/logout/reset + route protection.
  - **Depends on**: None.
  - **Governed by**: C-03 (Supabase Auth), C-04 (TDD), C-06 (conventional commits).
  - **Spec dir**: `specs/001-user-login/` — EXISTS, non-empty, `spec.md` 142 lines (real content).
- **Ledger 002 — Job Offer Analyzer**  ·  **Status**: undecided
  - **Description**: Core job-offer analysis & side-project proposal feature (LLM-based).
  - **Depends on**: 001 (in-progress — dependency exists, not abandoned ✅).
  - **Governed by**: C-02, C-03, C-04.
  - **Spec dir**: *(none recorded — pre-commitment)*
- **Disk `specs/002-public-home-login-button/`**  ·  *(no ledger entry)*
  - Full artifact set: spec.md (140 lines), plan.md, research.md, data-model.md,
    contracts/header-auth-ui.md, quickstart.md, tasks.md (29 tasks, 0 complete), checklists/.
  - Spec "Amends: `specs/001-user-login/spec.md`" → it iterates on the login feature.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 🎯 | orphan-spec | `specs/002-public-home-login-button/` is a fully-specced feature (spec + plan + research + data-model + contracts + quickstart + 29 tasks, 0 done) with **no matching ledger entry**. A complete spec exists off-roadmap, which breaks governance (specs should be tracked by the roadmap). | Add a new ledger entry for it via `/speckit.roadmap.write`. Suggested standing: `specced` (design complete) — not `in-progress` until the 29 tasks start. Record `depends-on: 001` and `Spec dir: specs/002-public-home-login-button/`, governed by C-02 + C-04 (Tailwind/strict + TDD/e2e). |
| F2 | 🎯 | status-drift / numbering collision | The ledger's `002` slot is **"Job Offer Analyzer"** (undecided, no dir). The disk's `002-...` directory is **"public-home-login-button"** (different feature). The `002` prefix is now ambiguous — two distinct features share the ordinal. | When adding the orphan (F1), resolve the numbering collision: either (a) enter the orphan as `003 — Public Home Page with Header Login Button` and leave the Job Offer Analyzer entry at `002`, or (b) renumber the Job Offer Analyzer entry to a later free slot. The disk dir name (`002-public-home-login-button`) stays as-is — spec directory names and ledger ordinals are independent, but the collision should be disambiguated in the ledger narrative. |
| F3 | 🤔 | status-drift | 001 is marked `in-progress`, but its own Notes record "41 tasks, 0 of 41 completed". All design artifacts are present (spec/plan/tasks/research/data-model/contracts/quickstart), which is the signature of `specced` rather than `in-progress`. No code tasks are checked off. | Confirm whether implementation has actually started. If not, advancing the label to `specced` (or keeping `in-progress` only if work is actively underway) keeps the ledger honest. Apply via `/speckit.roadmap.write`. |
| F4 | 💡 | dependency linkage (informational) | Once the orphan (F1) is added, its spec states it **amends** `specs/001-user-login/spec.md`. That cross-cutting "amends/iterates-on" relationship is not expressible in today's ledger schema (only `depends-on` exists). The 001 → public-home linkage would otherwise be invisible. | When writing the orphan's entry, note the "amends 001" relationship in its Notes, and consider adding a reciprocal note on the 001 entry so the iteration chain is discoverable. |

**Drift categories found**: orphan-spec (F1) · status-drift / numbering collision (F2, F3) ·
dependency linkage (F4, informational).

**Categories checked and clean**: phantom-entry (none — 001 lifecycle dir is present and non-empty;
002 Job Offer Analyzer has no dir pointer, which is correct for `undecided`) · status-lagging in the
pre-commitment sense (none — the disk `002-...` dir does NOT correspond to the `undecided` Job
Offer Analyzer entry, so Job Offer Analyzer correctly stays undecided) · abandoned-but-active (no
deferred/abandoned entries exist) · dependency-contradiction (Job Offer Analyzer depends on 001,
which exists and is not abandoned ✅) · superseded-ADR (ADR directory `docs/adr/` is absent → check
skipped per spec).

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 2 |
| 💡 Recommendation | 1 |
| 🤔 Question | 1 |
| **Total** | **4** (cap 50 — no overflow) |

## Recommended Actions *(proposed only — never auto-applied)*

1. **F1 + F2 (🎯)**: Run `/speckit.roadmap.write` to add the orphan as a tracked entry and resolve
   the `002` numbering collision. Suggested addition:
   - **`003 — Public Home Page with Header Login Button`** · Status: `specced`
   - Outcome: the home page is public (no forced login) and a state-aware "Log in" / account
     control appears in a shared site header.
   - Scope (in): make `/` public via `PUBLIC_ROUTES`, split `AUTH_PAGES` from `PUBLIC_ROUTES`,
     add a shared `SiteHeader` + `HeaderAuth` to the root layout, server-resolve session to avoid
     a logged-out→logged-in flash.
   - Scope (out): social login, profile management, email verification (unchanged from 001).
   - Depends on: 001 (amends the login feature's routing).
   - Governed by: C-02 (Next.js/strict/Tailwind), C-04 (TDD via Playwright e2e per user request).
   - Spec dir: `specs/002-public-home-login-button/`.
   - Notes: Amends spec 001; Playwright is the test layer (vitest not installed). 29 tasks, 0
     complete. Leaves the ledger's existing `002 — Job Offer Analyzer` at its current `002` /
     `undecided` standing, removing the collision.
2. **F3 (🤔)**: In the same `/speckit.roadmap.write` pass, verify 001's true implementation state.
   If the 41 tasks have not started, set 001 to `specced`; keep `in-progress` only if work is
   actively underway. Update the stale "0 of 41 completed" note to match reality.
3. **F4 (💡)**: When adding the orphan entry, record "amends 001" in its Notes and add a
   reciprocal note to the 001 entry ("amended by 003") so the iteration chain is discoverable.

---

**Severity Legend**: 🎯 Must-Address (blocks a clean roadmap) · 💡 Recommendation (strongly
suggested) · 🤔 Question (needs a decision). **This report is read-only**; apply any roadmap
changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.sync`.*